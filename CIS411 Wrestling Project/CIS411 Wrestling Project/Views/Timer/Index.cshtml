@model IEnumerable<CIS411_Wrestling_Project.Models.Match>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MatchTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HomePoints)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AwayPoints)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MatchTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HomePoints)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AwayPoints)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
